<UserControl x:Class="SuiviPortefolio.Transactions.TransView.TransView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="170"/>
            <RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="TRANSACTIONS" Style="{StaticResource TitleH1Style}" Margin="0,0,0,10"/>
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <ComboBox ItemsSource="{Binding Cotations}" SelectedItem="{Binding CotationSelectionnee, Mode=TwoWay}">
                <ComboBox.ItemTemplate><DataTemplate><StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Symbol}" FontWeight="Bold" Width="100"/>
                    <TextBlock Text="{Binding Name}"/>
                </StackPanel></DataTemplate></ComboBox.ItemTemplate>
            </ComboBox>
            <Button Grid.Column="1" Content="Rechercher une cotation" Click="RechercherCotation_Click" Margin="8,0,0,0" Padding="8,3"/>
        </Grid>
        <ListBox Grid.Row="2" ItemsSource="{Binding TransactionsRecentes}" SelectedItem="{Binding TransactionSelectionnee, Mode=TwoWay}">
            <ListBox.ItemTemplate><DataTemplate><TextBlock>
                <Run Text="{Binding TransDateTransac, StringFormat={}{0:g}}"/><Run Text="  "/>
                <Run Text="{Binding TransType}"/><Run Text="  "/><Run Text="{Binding TransQte}"/>
                <Run Text="  "/><Run Text="{Binding TransPrix, StringFormat={}{0:N2}}"/><Run Text="  "/><Run Text="{Binding CompteNom}"/>
            </TextBlock></DataTemplate></ListBox.ItemTemplate>
        </ListBox>
        <GroupBox Grid.Row="3" Header="Transaction" Margin="0,10,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions><ColumnDefinition Width="140"/><ColumnDefinition Width="220"/></Grid.ColumnDefinitions>
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <TextBlock Text="Compte :" Margin="0,0,8,8"/><ComboBox Grid.Column="1" ItemsSource="{Binding Comptes}" SelectedItem="{Binding CompteSelectionne}" Margin="0,0,0,8">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding CpteNom}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <TextBlock Grid.Row="1" Text="Type :" Margin="0,0,8,8"/><ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding TypesTransaction}" SelectedItem="{Binding TypeTransaction}" Margin="0,0,0,8"/>
                <TextBlock Grid.Row="2" Text="Quantité :" Margin="0,0,8,8"/><TextBox Grid.Row="2" Grid.Column="1" Text="{Binding QuantiteTexte, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                <TextBlock Grid.Row="3" Text="Frais :" Margin="0,0,8,8"/><TextBox Grid.Row="3" Grid.Column="1" Text="{Binding FraisTexte, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                <TextBlock Grid.Row="4" Text="Total :" Margin="0,0,8,0"/><TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding TotalTexte}"/>
            </Grid>
        </GroupBox>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Annuler" Click="Annuler_Click" Width="90" Margin="0,0,8,0"/>
            <Button Content="Enregistrer" Click="Enregistrer_Click" Width="100"/>
        </StackPanel>
    </Grid>
</UserControl>

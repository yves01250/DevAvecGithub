<Window x:Class="SuiviPortefolio.Comptes.CompteView.CompteEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Compte"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="220"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                   Text="{Binding Titre}"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Text="Nom :" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <TextBox x:Name="NomTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="2" Text="Type :" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <ComboBox x:Name="TypeComboBox" Grid.Row="2" Grid.Column="1"
                  ItemsSource="{Binding TypesCompte}" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="3" Text="Devise :" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <TextBox x:Name="DeviseTextBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="4" Text="Solde :" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <TextBox x:Name="SoldeTextBox" Grid.Row="4" Grid.Column="1" Margin="0,0,0,8"/>

        <CheckBox x:Name="DefautCheckBox" Grid.Row="5" Grid.Column="1"
                  Content="Compte par défaut" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Annuler" IsCancel="True" Width="90" Margin="0,0,8,0"/>
            <Button Content="Enregistrer" IsDefault="True" Width="100"
                    Click="Enregistrer_Click"/>
        </StackPanel>
    </Grid>
</Window>
